<% if post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(post.errors.count, "error") %> prevented you from creating your post:</h2>
    <ul>
    <% post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= form_for post do |p| %>
  <%= p.label :title %>
  <%= p.text_field :title %>
  <br>
  <%= p.label :content %>
  <%= p.text_area :content %>
  <br><br>
  <%= p.submit %>
<% end %>
